import { PageContainer } from '@/app/components/templates/PageContainer';
import CreateAccountContainer from './createAccountCompnent';

export default function CreateAccountPage() {
  return (
    <PageContainer>
      <CreateAccountContainer />
    </PageContainer>
  );
}
